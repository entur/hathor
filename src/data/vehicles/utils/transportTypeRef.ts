// Shared NMR codespace + completeness-gate regex for Vehicle.transportType.id (consumed by vehicleFormState.canSubmit).
export const TRANSPORT_TYPE_PREFIX = 'NMR:VehicleType:';
export const TRANSPORT_TYPE_REF_PATTERN = /^NMR:VehicleType:\d+$/;
